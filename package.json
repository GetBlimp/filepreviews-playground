{
  "name": "FilePreviewsPlayground",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf parse/public && mkdir -p parse/public",
    "build": "npm run clean && cp index.html parse/public && cp app.js parse/public && cp -r bower_components parse/public",
    "deploy": "npm run build && cd parse/ && parse deploy"
  },
  "repository": {
    "type": "git",
    "url": "git@git.blimp.io:FilePreviews/playground.git"
  },
  "author": "Giovanni Collazo <hello@gcollazo.com>",
  "license": "MIT"
}
